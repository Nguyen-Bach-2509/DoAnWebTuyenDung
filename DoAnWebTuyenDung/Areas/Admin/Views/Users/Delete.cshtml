@model DoAnWebTuyenDung.Models.User

@{
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<h2 class="text-center mt-3" style="color:#ff4d4d">Xóa Người Dùng</h2>

<div class="container mt-4">
    <div class="card border-0 shadow-sm mx-auto" style="max-width: 800px;">
        <!-- Tiêu đề và màu nền của card-header -->
        <div class="card-header text-center" style="background-color: #ff4d4d; color: #ffffff;">
            <h5 class="mb-0" style="color:#ffffff">Bạn có chắc chắn muốn xóa người dùng này không?</h5>
        </div>
        <div class="card-body" style="background-color: #f8d7da;">
            <div class="text-center text-danger fw-bold mb-4">Thông tin người dùng sẽ bị xóa vĩnh viễn.</div>

            <!-- Thông tin người dùng -->
            <dl class="row justify-content-center">
                <dt class="col-sm-3">Tên người dùng:</dt>
                <dd class="col-sm-7">@Html.DisplayFor(model => model.username)</dd>

                <dt class="col-sm-3">Mật khẩu:</dt>
                <dd class="col-sm-7">@Html.DisplayFor(model => model.password)</dd>

                <dt class="col-sm-3">Email:</dt>
                <dd class="col-sm-7">@Html.DisplayFor(model => model.email)</dd>

                <dt class="col-sm-3">Vai trò:</dt>
                <dd class="col-sm-7">@Html.DisplayFor(model => model.role)</dd>

                <dt class="col-sm-3">Ngày tạo:</dt>
                <dd class="col-sm-7">@Html.DisplayFor(model => model.created_at)</dd>
            </dl>

            <!-- Form xác nhận xóa -->
            @using (Html.BeginForm())
            {
                @Html.AntiForgeryToken()
                <div class="text-center mt-4">
                    <input type="submit" value="Xóa" class="btn btn-danger" />
                    @Html.ActionLink("Quay lại", "Index", null, new { @class = "btn btn-secondary ms-2" })
                </div>
            }
        </div>
    </div>
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
